import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Attendance Dashboard - BLE Tracking System',
  description: 'Real-time location-based attendance tracking using BLE beacons and ESP32 scanners',
  keywords: 'attendance, BLE, ESP32, tracking, classroom, students',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
